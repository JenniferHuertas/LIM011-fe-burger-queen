/* import { shallowMount } from '@vue/test-utils';
import Menu from '@/components/Menu.vue';
import Vuex from 'vuex';

describe('Menu.vue', () => {
  it('aumentarProducto', () => {
    const state = { listaProductos: [] };
    aumentarProducto(state);
    expect(state.listaProductos()).toBe([]);
  });
});
*/
